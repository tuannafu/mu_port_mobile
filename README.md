# mu_port_mobile

Android port workspace for Main5.2.

Current status:
- Initial Android Gradle/NDK scaffold added under `android/`
- Source audit completed
- Next step: isolate Win32 bootstrap, input, timing, path, and rendering boundaries from the legacy client before wiring real game code into Android

This repo is intentionally kept lightweight first so progress can be reviewed incrementally without pushing the original large Windows source history.
